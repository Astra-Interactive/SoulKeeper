main: '${main}'
prefix: '${prefix}'
description: '${description}'
name: '${name}'
version: '${version}'
authors: [ "${authors}" ]
author: "${author}"
website: "${url}"
api-version: 1.18
softdepend: [ packetevents ]
depend: [ ]
loadbefore: [ Towny ]
libraries: []
commands:
  souls:
  skreload: